# 미용실 AI 마케팅 서비스 - 팀 공용 패키지 기준
# 최종 갱신: 2026-08-04
#
# [기준] Google Colab 환경에 맞춥니다.
#   Colab의 torch는 세션마다 재설치해야 해서 바꾸기 어렵고,
#   팀 VM은 한 번 설치하면 유지되므로 VM을 Colab에 맞췄습니다.
#
# [검증 완료] 아래 두 환경에서 실제로 확인했습니다.
#   - 팀 VM  : salon-ai-1 / Ubuntu 24.04 / Python 3.12.3  / NVIDIA L4 23,034MiB
#              드라이버 580.173.02 (CUDA 13.0)
#   - Colab  : Python 3.12.13 / NVIDIA L4 22,563MiB
#   두 환경 모두 아래 버전에서 diffusers 파이프라인 import 성공(SDXL / SDXL Inpaint).
#
# [주의] VRAM 한계 실험은 Colab 기준(22.5GB)으로 잡으세요.
#        Colab에서 돌아가면 VM에서도 돌아갑니다. 반대는 보장되지 않습니다.

# --- torch ---
# Colab에는 이미 설치돼 있어 아래 설치 시 건너뜁니다.
# 새 환경(예: 팀 VM 재구축)에서는 반드시 cu128 인덱스로 설치하세요.
#   pip install torch==2.11.0 torchvision --index-url https://download.pytorch.org/whl/cu128
torch==2.11.0

# --- 생성 모델 ---
diffusers==0.39.0
transformers==5.14.1
accelerate==1.14.0
safetensors==0.8.0
huggingface_hub==1.26.0
peft==0.19.1

# --- 이미지 처리 ---
# Colab은 opencv-python(4.13.0)이 기본 설치돼 있어 아래 설치 시 건너뛰어도 됩니다.
# 서버(팀 VM)는 GUI가 없어 headless 버전을 씁니다. API는 동일합니다.
numpy==2.0.2
pillow==12.2.0
opencv-python-headless~=4.13.0

# --- 문구 생성 / 설정 ---
openai==2.53.0
python-dotenv==1.2.2
