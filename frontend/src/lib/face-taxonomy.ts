// 얼굴 교체 옵션의 보기값 정의 — Discussion #69 확정안 기준
//
// 필수 3개(국적·성별·연령대)와 세부 4개(얼굴 스타일·표정·스킨 톤·메이크업) 모두
// 고정 목록에서만 고른다. 백엔드 prompt_map.py 가 한글 값을 키로 영문 프롬프트를
// 찾기 때문에, 목록에 없는 한글이 오면 에러 없이 그 값만 조용히 빠진다.
// 자유 입력은 8/12 수민님 회신으로 닫혔다 — 칩 선택만 남긴다.
//
// 값은 한글 그대로 전송한다. 영문 변환은 백엔드 몫이다.
// 프론트가 영문 어휘까지 정하면 모델 프롬프트를 손볼 때마다 화면을 같이 고쳐야 한다.

import { GENDERS } from "@/lib/style-taxonomy";

/** 세부 항목을 고르지 않았다는 뜻의 칩. 전송값은 빈 문자열이다. */
export const FACE_NONE_LABEL = "선택 안 함";

/** 성별. 상담 화면과 의미가 같아 그대로 재사용한다. */
export const FACE_GENDERS = GENDERS;

/**
 * 국적. 손님 대부분이 한국인이라 첫 칸에 둔다.
 * 권역(동아시아)보다 국가 단위여야 백엔드가 프롬프트로 바꿀 때 모호하지 않다.
 */
export const FACE_ETHNICITIES = [
  "한국인",
  "일본인",
  "중국인",
  "동남아시아인",
  "서양인",
  "흑인",
  "라틴계",
  "중동인",
] as const;

/**
 * 연령대. 10살 단위 5종이다.
 * 초반·후반을 나눠도 결과가 같았다 — 나이는 골격에서 드러나는데 그 영역이 마스크에
 * 묶여 있어 프롬프트가 개입할 여지가 좁다(#66 3.2). 골라도 안 바뀌는 선택지를
 * 남겨두면 고른 사람이 헷갈린다.
 */
export const FACE_AGE_GROUPS = [
  "10대",
  "20대",
  "30대",
  "40대",
  "50대 이상",
] as const;

/**
 * 얼굴 스타일. 이목구비만 담당한다 — 인상·무드는 FACE_EXPRESSIONS 몫이다.
 *
 * 앞 7개는 동물상이다. 국내에서 얼굴 인상을 부르는 표준 언어이기도 하고,
 * 분류 기준이 눈꼬리 방향과 코끝 모양이라 이목구비를 직접 가리킨다.
 * 눈·코는 마스크 안쪽이라 프롬프트가 닿는다.
 */
export const FACE_STYLES = [
  "강아지상",
  "고양이상",
  "토끼상",
  "사슴상",
  "여우상",
  "곰상",
  "늑대상",
  "중성적인",
  "개성 있는",
] as const;

/**
 * 표정. 얼굴 스타일과 나눠 둔 축이다.
 * 인상 어휘를 한 축에 몰면 결과가 무표정/웃는 표정 두 가지로 수렴한다(#66 2.2).
 * 표정은 눈·입이라 마스크 안쪽이고, 반영이 확인된 축이다.
 */
export const FACE_EXPRESSIONS = [
  "무표정",
  "자연스러운 미소",
  "활짝 웃는",
  "수줍은 미소",
  "눈웃음",
  "편안하고 여유로운",
  "밝고 에너지 넘치는",
  "살짝 입꼬리만 올린 미소",
  "도도한",
  "차갑고 날카로운 눈빛",
  "강렬한 응시",
  "쿨하고 무심한",
  "생각에 잠긴",
] as const;

/**
 * 스킨 톤. 밝기만 고른다.
 * 질감(매트·윤광)은 화장 마감이라 메이크업이, 나이는 연령대가 이미 하고 있다.
 * 세 단계로만 나눈 이유 — 붙어 있는 값은 결과에서 갈리지 않는다(#66 2.2).
 */
export const FACE_SKIN_TONES = [
  "밝은 톤",
  "자연 톤",
  "태닝 톤",
] as const;

/**
 * 메이크업. 립이 가장 확실하게 반영되는 축이라 컬러를 이름으로 나눴다(#66 2.2).
 *
 * 톤(쿨톤·웜톤)은 넣지 않는다 — 사진 속 헤어 컬러가 이미 톤을 정해둔 상태다.
 * 이마·관자놀이까지 올라가는 화장도 넣지 않는다 — 마스크 밖이라 그려질 자리가 없다.
 */
export const FACE_MAKEUPS = [
  "노메이크업",
  "꾸안꾸",
  "속광",
  "물광",
  "벨벳 블러",
  "펄 스킨",
  "프로스티드 아이",
  "취기 블러셔",
  "썬번",
  "말린 장미",
  "벽돌빛",
  "프로스티드 립",
  "체리 레드",
  "다크 로맨틱",
  "또렷한 눈매",
  "남성 그루밍",
] as const;

/**
 * 필수 3개의 허용 목록. 서버 검증이 이 배열을 그대로 본다.
 * 화면과 검증이 같은 출처를 봐야 "화면에선 고를 수 있는데 서버가 거부"가 생기지 않는다.
 */
export const FACE_REQUIRED_ALLOWED = {
  ethnicity: FACE_ETHNICITIES,
  gender: FACE_GENDERS,
  age: FACE_AGE_GROUPS,
} as const;

/**
 * 세부 4개의 허용 목록. 자유 입력이 닫히면서 필수와 같은 방식으로 검증한다.
 * 세부는 안 고를 수 있으므로 빈 문자열도 통과시켜야 한다 — 검증 쪽에서 처리한다.
 */
export const FACE_OPTIONAL_ALLOWED = {
  face_style: FACE_STYLES,
  expression: FACE_EXPRESSIONS,
  skin_tone: FACE_SKIN_TONES,
  makeup: FACE_MAKEUPS,
} as const;
